1、harbor登录问题
 kubectl create secret docker-registry registry-secret --namespace=default --docker-server=https://www.j116.cn --docker-username=hy --docker-password=Hy_888888 --docker-email=1621740748@qq.com
修改deployment
    spec:
      containers:
      - image: private-registry.domain.com/test-private/service-test:1
        name: service-test
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: registry-secret


2、代码拉取登录问题解决
		checkout scm

3、pipeline中的secrect 需要在jenkis中进行配置

